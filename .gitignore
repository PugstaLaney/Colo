# Secrets — never commit these
.env

# Vector database — binary files, too large for git
data/

# Python cache
__pycache__/
*.pyc

# Local design notes — personal reference, not for public repo
DESIGN_NOTES.md
